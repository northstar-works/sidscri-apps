

I want to be able to bulk add cards to existing decks using ai generator. in ai generator i want the option from dropdown to select a deck to.





ip##
Edit Deck>AI Generator>AI Deck Generator>Keywords> Generate button needs to be moved to where it is not touching the box above and reduced in size by only a 1/4.  
Add a "Preview All" button on the same line, right side of page with a "Cancel" button to go back to "Switch" page. Location of button will be same as in photo 2. keeping "Generate" button on the left side of page. 
Text put in "Instructions for the AI (optional)" is persistent, needs to be cleared out on exit, or according to "Inserted Example format" chosen.  
Text in "Instructions for the AI (optional)" has too many extra lines when "Inserted Example format" option is chosen. 
Want a "Clear" button on far right of "Inserted Example format" button on that page to clear out any typed or "Inserted Example format" text
## 

(**Verify works** 2.) notifying of adding duplicates with action prompt (add duplicate. replace duplicate , cancel).**


ip##
.##

###Exe build###



###Universal###



###Android App#



##Package##



##tool##


Wish the new updated KenpoFlashcardsWebServer_Packaged changelog and readme more closely resembled the add, fix, changes, simular to whats in the KenpoFlashcardsWebServer changelogs add, fix, changes.

##To Change/Add##

Add an auto backup of data folder to %local_user%\...\DataBackups\"Data_Auto_%Date%_%Appversion%\data".zip\ Changes on demand, keeping last 10 per user to be restored, another setting for auto backup: off>every 30min>1 hour>2 hours>6 hours(default)>1 Day>2 days.



----Webserver----





****DONE****

If successfully pulled from "\%appdataLocal%\Advanced Flashcards WebApp Server\data\", backup current "\root\data\" to "\sidscri-apps\DataBackups\"WebServer_Packaged_Data_%Date%\data".zip\" then replace current "\root\data\" with "\packaging\build_data\" data.  Doing this will keep the next package being built updated with the most up-to-date data, in case it gets installed on a new device. If there isnt \Advanced Flashcards WebApp Server\data folder in %local_user%, which normally means its a fresh install, than skip and use "\root\data\" instead of "\packaging\build_data\".   

Verify when "\packaging\build_data\" is present that "2. build_exe.bat" is pulling from "\packaging\build_data\" when "\packaging\build_data\" files are newer than "\root\data\"., if "\packaging\build_data\" isnt there than default to "\root\data\" Any overwrites needs to be backed up first. Log the upgrade or installed version to version, files touched, files skipped, files replaced, etc in both sidscri-apps\logs\Install\updates<or>installs\log to have pc>user>version>dates and "\%appdataLocal%\Advanced Flashcards WebApp Server\data\logs\log to have version>dates"


still needs to after build backup "\sidscri-apps\KenpoFlashcardsWebServer_Packaged\data" and Replace with newer data in KenpoFlashcardsWebServer_Packaged\packaging\build_data\ flagged as being pulled from "C:\Users\Sidscri\AppData\Local\Advanced Flashcards WebApp Server\data".

Change log location in Local\Advanced Flashcards WebApp Server\data\log\ to existing \root\log\ folder

Need a flag to let builder know that data was pulled from Appdatalocal into build_data so build_data will be used in a new install, if flag is negative then use packaged data folder.

During update of app to PC, existing data files win over unpackaged data files. if there is a way to know which specific file is newer and better, than override, but backup data to %local_user%\...\DataBackups\"Data_Updated_%Date%_%Appversion%\data".zip\

deck image feature: I will want an option to add images to study decks as an option when creating or editing decks. For Kenpo, I want "Kenpo Vovabulary Study Flashcards.png" used as its logo on main page as pictured and by default without adding optional image, use "Advanced Flashcards logo.png".

Windows icon changes: Create a dedicated location for Icons and logos in KenpoFlashcardsWebServer_Packaged\static\res\webappservericons\. Create new app icon files using the "Advanced Flashcards WebApp Server Icon.png" photo to remap all current kenpo icons that show on a windows system, tray, desktop, add remove programs list, etc. Dont touch any that are being used by the web app in browser or the original windows server project. I will sync those updates later after editing on webserver project. 


When i refresh page it always changes back to kenpo with kenpo image.  I dont think default deck actually makes defaulted deck stay default after refresh.

I want to add in make3_zip_KenpoFlashcardsWebServer_Packaged-Zip.bat if version.json contains "webserver_version": "8.0.2",
  "webserver_build": 47, add "_ws%webserver_version%-%webserver_build%" to the zip name

Forgot to add but might have been addressed in recent 8.1.1 upgrade already. keep same version, update docs:  kenpo_words.json is now located at C:\Users\Sidscri\Documents\GitHub\sidscri-apps\KenpoFlashcardsWebServer\data\kenpo_words.json &fix5.png and fix7.png& Then I need the sync tool to make sure mapping for synced (replaced files from webserver) that are now synced on packaged project for exe build has no reference to \KenpoFlashcardsWebServer\, but is mapped to \KenpoFlashcardsWebServer_Packaged\ instead, since those file now reside within KenpoFlashcardsWebServer_Packaged project now. 

I want kenpo_words.json to live and get mapped from KenpoFlashcardsProject-v2\app\src\main\assets\ to KenpoFlashcardsWebServer\data\ instead.  That means KenpoFlashcardsProject needs to also change its grab to KenpoFlashcardsWebServer\data\ if appicable

Change "Kenpo Flashcards" to "Advanced Flashcards"; "Study Flashcards" to "Advanced Flashcards", except for ("Study Flashcards" {Name of current Deck} "Cards loaded: 89") on main pages. Keep as it is.

Other users non built-in decks should not be available by default on new or other profiles.  Sidscri created Basic Spanish Words..., only sidscri should have it in his deck, unless admined to any specific user.

Add a save current Custom Deck to Custom deck saves page, make sure there is a way to chose a saved deck to study when desired, 

Change WebApp icon, Now that the apps name has changed to "Advanced Flashcards WebApp Server"

3.) In "User Deck Access" section, when a user is selected and a deck is selected there needs to be a status of that decks access for that selected user, and the "Unlock"/"Lock" and disable and enable built-in buttons should only be available for the only action that can be made. 

4.) In "Built-In Deck Management", there isn't a way to add built-in decks by default, there is a "Remove Built-In" button on kenpo (which is great), but how to re-add kenpo if "Remove Built-In" button is pressed on kenpo. make minor version upgrade.

5.) admin force of password refresh for john smith, he logged in using 123456789, but it did not ask for a force a password change, before logging in. After admin reset of password, if a user tries a password that is not 123456789 a message should appear on that login window that password change is required, then add fields to reset password and to submit.  I want version and build text on corner of login window. How is remembering device user logged into handled, to reduce the amount of times user has to login?

6.) In "Create New Deck" section I want a method of adding cards to be added right below Description.  After creating a new deck and with that option selected it will go to that methods page ie. Keyword, Photo, Document.  If keyword is chosen it will auto search for max of 25 cards using deck name and description., the other methods will open thier photo or document upload window. Once added, I want the search keyword cleared and max cards to default to 25. I noticed after sucessfully adding 51 state capitals, the Search keywords and max cards persisted.

This works fine on pc, and use to work great on all devices,  but on any other device its not very mobile friendly, im also not liking the on main screen image moving around.   I want it to stay in place, maybe even reduce size by 15%. if title is to long than block the deck title with ....  The cards are not showing up even though I see the total of cards.  I want to stop having to login everytime.  I want it to remember me on this browser or device.  Window is to small on edit user, and I cant see kenpo as being owned by me.  I cant even edit my kenpo deck. I want kenpo to be like any other deck that I own. The only difference is the built in marker. If I remove built in than, it will not exist on new users, existing user still will unless I force remove them, that deck access feature also does not work.  Please check and see how we can make it run fully operational, smooth.  Look over the package and see how the new tool syncs webserver to package.  This sync process works fine with the tool.

When adding decks, I dont want keywords required, if no keyword inputted than use name and or description. Name is required, description is not, but it auto filles with deck name when empty. Option to auto ai fill/edit description in deck edit. Definitions need to be definition only, not adios = "a way to say goodbye" but just Goodbye; or Austin = "Capital city of Texas" but just Texas.  Add an option in decks edit deck settings to make definition more descriptive unique setting per deck. Logged in as sidscri admin, i get admin access required.  When syncing verify the other end is the server running at C:\Program Files\Advanced Flashcards WebApp Server / C:\Users\Sidscri\AppData\Local\Advanced Flashcards WebApp Server.  packaged and android projects and exe Files are provided.

Thats the current verision .zip i corrected the build 55>54.  Use these photos to change the buttons and card window from before to after(desired). The edit user window needs to be wider so I can see all the user deck Not granted or Granted check and text. If they are owned by the user they are granted access by default, that needs to be fixed, plus I see 0 cards on the decks i own yet in study pages I see the correct card counts.  Admin>Deck page nothing works properly in any of the sections. Disable and enable built-in for user buttons dont work nor do the unlock/lock.  I need a section to change ownership of decks in Admin>Deck> Deck Ownership.  What is the difference between "Admin>users>edit users>Deck Access" and "Admin>Deck>"User Deck Access" Unlock and Lock function.  There needs to be a marker letting us know if bulit-in is active or not and if unlocked is active for the Select user...>Select deck... or not. If unlock and lock have same function as "Admin>users>edit users>Deck Access" than rename Buttons to Allow and Deny buttons with "Access" text static to the right of the last (Deny) button.  With something telling us what access status they have. remove check mark in edit user. just have the "granted" and  "not granted" text synced with "Admin>Deck>"User Deck Access" status.   Keep same version just edit .md to reflect these changes

There are not logs to view in Admin>logs page.  App logs should atleast show since the server is or has been ran. KenpoFlashcardsWebServer\logs\Server>user activity. Dont allow download if log is empty or 0k, I want every run a different server and error log saving the previous log. Users Activity log should be continuous unless cleared, then saved before creating a new user activity log.  Wish to be able to expand Admin statistic tiles to show more in depth information per tile, without changing its current look while no tile is expanded

8.) In admin>overview>"Total Users" to show general info about the users, >"Study Deck" to show all decks and their info.  For those and the other >"Total Cards", >"Breakdowns" lets make it a fun experience to look at when clicked on.

2.) in Custom Set Settings the random number field is still ontop of Add random button, field needs to be way shorter.

Remove disable or enable built-in for users function from this page.  Built-In Deck Management does that already.  I want to be able to remove kenpo from built-in, right now I can not.  in that same danger area I want to be able to remove Decks from server. Confirm all danger area changes with admin users password.  In users> I want a delete user button on bottom right with confirmation with password. Sidscri can not be removed.  

1.) Need to also add in webserver project to only be seen in packaged installed versions "User: ***" menu, about, and system information, and anywhere else it should be seen its Web Server Version: v8.1.0. 7.) 

verify that logs can be made to show in Admin>logs page>"server logs"> App Logs, Error Logs, User Activity (only logs)

I want an indepth documentation with charts and table on your understanding on how the process works going from Webserver project >using the one way "SyncTool-WebServerToPackaged" tool(details in what it does)>Webserver with updated features gets one way synced to webserver packaged > EXE > installed server on windows system>directory tree> showing the Connections between android app web sync > installed server on windows system. After outputting your results to download. I want a separate doc showing issues found during your analysis and how to apply changes now or at a later time.  I still want a tool to bring new features and updates from webserver to android app with a push of a button, while still allowing the app to be vital. Pass github push process, change its version #, readme and changelog, version.json.

Sanity check current version of webserver zip:  

Continue implementing expandable stat tiles and logging overhaul. "There are not logs to view in Admin>logs page.  App logs should atleast show since the server is or has been ran. KenpoFlashcardsWebServer\logs\Server>user activity. Dont allow download if log is empty or 0k, I want every run a different server and error log saving the previous log. Users Activity log should be continuous unless cleared, then saved before creating a new user activity log.  Wish to be able to expand Admin statistic tiles to show more in depth information per tile, without changing its current look while no tile is expanded"

Also fix, in Custom Set Settings the random number field is still ontop of Add random button, field needs to be way shorter and not over lapping "pick random cards to add" text or the button.  Make only 1/4 the size of current making sure 1000 could fit in field. make "pick random cards to add" text to fit in single line. 

Custom set setting>manage cards and Saved sets bages is bare.  Needs functionality buttons to manage. be able to add or remove cards from list. Save current custom deck, edit, delete a saved custom deck, to be able to switch between custom decks, create a custom deck will saving the current, Custom name field. The default custom deck should as default show up in managed cards, setting should have Save this custom deck. 

Adding a new study deck should not assume im still thinking about spanish words.  While basic spanish words.. deck was active I made a new deck by using the quick create new deck putting "fastfood chains types of food" in name field only, method keywords.  Every word the ai found was a spanish type of food. tacos, enchalatas, etc.  

When in a study deck, and you want to add/delete a card, the target Deck should always pre fill with the current deck.  Right now it just defaults to kenpo. Cards are no longer being added to custom deck when flagged. I want Unlearned: 50 | Unsure: 0 | Learned: while in custom to reflect status of custom deck not the main decks.  

I want the ai to be able to smartly keep definitions to one word if possible when auto adding cards, like, state capitals, word translations, but be able to allow longer definitions like for studying science i.e  Term: Nucleus Definition: 
"The central and most important part of an object, movement, or group, forming the basis for its activity and growth."  But want it to smartly keep one word definition if thats all it needs. I want an option in a new deck specific setting button like custom deck button, to be able to make that deck only short answers. like cat, dog, Washington D.C, Slow down, Lake Tawakini, prime midian, etc. incases definitions keep making to long. 

Old icons are still showing up in desktop shortcut(in properties the new are in change icon) and showing in task manager processes &&fix.3.png&&

I am wanting to see what I can bring feature wise, from web server project 2 the Android App. Of course, there are some features that shouldn't even be in and and or the app that the Windows version Has, I get that. But I also just wanna make sure that while I am adding and fixing the web, version, I am not forgetting to implement that on the Android app. Like, when I add a deck window on the, web server. I would like to be able to see that deck on the app. Then I log in. And it thinks and now that deck is now showing up. And, alternative alternatively, it goes around I want you to analyze both of them, And please produce a document listing the differences. And also going through and finding out Steps in being able to Make the Android app Feature wise, look like the web server app. I love the design of the Android app. So much. I do not want to change that. At all. I just wanna make sure that the same features that are on the web will be on Android without breaking the Android. And, also, Syncing. Making sure that The Android. Changes will reflect on the web server or the web server on the Android. I know we already have Sync web sync feature or the breakdown sync feature. Anything that has, you know, already built into Ben. I just wanna yes. So just for you to make a list. Of breaking down what the Android app does not have that the web server does have and I want for you to analyze and see okay, what can work on Android or what should not We put on the Android app And then also, I Give me on how you would implement it. I'm doing that. Thank you and if you feel confident enough, go ahead and Do it. And give me, please, a an updated project that implements all these changes. If you feel confident enough in doing that, which I think you do. That will be great. I would love to also have a tool that allows me to Update. The Android app. Whenever there are any updates on going forward. Now that once we get this Android app going per perfectly where everything is primarily up to date, Any changes when I do on the the, web app feature wise, will happen on The Android app.

### ❌ 1. Custom Set Random Number Field — Too Wide (Overlaps Text)
**Problem:** The random card count input (`.csRandomInput`) is supposed to be 52px wide, but the global CSS rule `input{min-width:280px}` overrides it, making it 280px — way too wide, overlapping the label text and button.  
**Root cause:** `.csRandomInput` is missing `min-width: auto !important` or `min-width: unset` to override the global input minimum.  
**What to say to reimplement:**
> "In styles.css, the Custom Set random number input (.csRandomInput) is broken because the global `input{min-width:280px}` rule overrides its width:52px. Add `min-width: auto !important;` to the `.csRandomInput` rule so it stays at 52px wide."

### ❌ 2. "Add Random" Button Should Say "Add Random Card"
**Problem:** The button in Custom Set Settings > Settings tab says "🎲 Add Random" but should say "🎲 Add Random Card" for clarity.  
**Root cause:** The button text in `index.html` was never updated.  
**What to say to reimplement:**
> "In index.html, find the button with onclick='pickRandomCardsToCustom()' that says '🎲 Add Random' and change its text to '🎲 Add Random Card'."

I want kenpo and spanish to be owned by sidscri, only kenpo to have built-in flagged, spanish will just be as it is a user created deck that no other user can access, unless I give it to them as an admin. Where did the the edit decks button go on spanish, guess it didnt show since I couldnt make me the owner.  Same goes for built-in settings and user deck access, no decks are showing up in drop downs.  &&Fix1.png, fix2.png

custom status Unlearned, unsure, learned isnt showing count shows 0 should be 10 after random 10 added and showing 9 in unlearned and 1 in unsure as expected.
"Custom > All (flat)..." "Custom" part of text should be what active deck status i am learning.
In "Custom Set Settings" each section box needs to be smaller so when available cards is expanded select all and save button will be on screen.
"Custom Set Settings" > "Setting" doesn't load until after I change page tabs.
Want "Custom Set Settings" to fit on any screen vertically and horizontally.
Why can't this window screen act like Admin > Edit Decks window page
Horizontally "Custom Set Settings" doesn't fit the screen, and I can't scroll. 

1.) Need to also add in webserver project to only be seen in packaged installed versions "User: ***" menu, about, and system information, and anywhere else it should be seen its Web Server Version: v8.1.0. 
2.) in Custom Set Settings the random number field is still ontop of Add random button, field needs to be way shorter.  Rename "Add Random" to "Add Random Cards". 
3.) In "User Deck Access" section, when a user is selected and a deck is selected there needs to be a status of that decks access for that selected user, and the "Unlock"/"Lock" and disable and enable built-in buttons should only be available for the only action that can be made. 
4.) In "Built-In Deck Management", there isnt a way to add built-in decks by default, there is a "Remove Built-In" button on kenpo (which is great), but how to re-add kenpo if "Remove Built-In" button is pressed on kenpo.

Add a setting button>"Show these errors in ui log(what ever these log types are)" toggle default to off.  Change Custom setting icon to something a little different to not get confused for the main "more" settings icon. 

Only in mobile device>portrait> In "Custom set settings">Manage Cards>"custom set" and "Available Cards" I want those sections to expand when clicked(unexpanded by default). One can be expanded at a time.  In windows browser and mobile device> landscape>Available Cards list is very bad needs to be fixed to show Checkbox name, cards term, in custom status.  Only on mobile devices>both orientations has the "custom set setting" page to big (Seems zoomed in look) not fitting well on the screen. 

Need to do a package/android app anaylisis to check android app live pull from/push to location. 
I need to make sure kenpo.json is being mapped from the correct place.  what is the connection with kenpo_words.json for android app, WebServer, packaging.  I need to figure out if its a single source of truth or co-synced source of truth for that file.  What happens to the files data down the process.


The custom available cards list is showing all cards from across study decks, which also has USA state capitals custom deck showing basic spanish... custom cards as well.  It needs to go back to where each study deck has its own custom decks.  Only cards from active study deck should be available in "manage cards">"Available Cards".  No "manage cards">"In Custom Set" cards are listed in this list, even though there are 10 or so state cards in custom and wrongfully spanish words as well.
When I click on the custom deck study(star) page icon next to "All" button, it appears to be marking and un marking shown cards in the study deck. That icons action should only goes to the custom study deck page.
On all devices, when "custom set setting" page opens setting is blank when I go to another section, then back to settings everthing appears that needs to be addressed.
When main screen loads or browser refreshes it hangs as pictured for a few seconds then loads deck, might be something slowing down it loading.
version bump to 8.5.3.3

I want a detailed well formatted summary of this conversation chronicled by version then subject of fix, add, change, if same subject was further address again through out the chat, I want it all added, but cited, to the final result of that specific fix, repair, change, add , citing what version that specific fix was applied in.  Meaning, if you added a feature that mostly worked, but there were a few bugs worked out, I want the last version that it worked to house the cited fix subject. listing any important directions, non coding steps to be made.  wiring, etc. Create in word if you could grab from the other chat from yesterday and today that would be awesome. If not I would end up doing the same with the other 2 or 3 chats as well as this one, then get you to combine any way, but doing it in one shot would make more sense. scanning the current project. Add a section with confirmed changes in chat, but not actually implemented in .zip. citing in what version it was suppose to be implemented.
Make sure any synced data that android app users uses for live syncing is now mapped to C:\Users\Sidscri\AppData\Local\Advanced Flashcards WebApp Server\data\ That is the true live location since going "live" on .exe build project. Make sure all api are directed towards localuser or program files or programdata, might need to relocate read and write files that might be in program files to programdata or localuserapp. Would like a server status in android app admin like the server has to check server running on local machine.
Also, I need to make sure kenpo.json is being mapped from the correct place.  what is the connection with kenpo_words.json for android app, WebServer, packaging.  I need to figure out if its a single source of truth or co-synced source of truth for that file.  What happens to the files data down the process.
Make sure any synced data that android app users uses for live syncing is now mapped to C:\Users\Sidscri\AppData\Local\Advanced Flashcards WebApp Server\data\ That is the true live location since going "live" on .exe build project. Make sure all api are directed towards localuser or its local machine installed location.
You might need to complete in steps if usage constraints does not allow to finish.

*In Stand-Alone WebServer*
This admin system app information section works on webserver project version. but does not here. On an exe installed version built from packaged I want in also want in the "version" field its Web Server Version 8.5.1 (build 54) its code coming from webserver project and the local EXE version from webserver packaged that is currently showing 6.0.0 (build 18). need to see how we can add Web Server Version in the build to be shown here and to fix the Released not showing up on this exe local version either.  Same needs to be applied to the version area of User:**** drop down.

App Version: 6.0.0 (build 18)
WebAppServer: 8.5.1 (build 54)



Every time I click Custom Deck Star Icon, need to fix the star custom marker on active card turns off and back on, but the card thankfully still stays in custom deck, which is good. 

Need to fix the custom deck star button again. When I click on it, it acts like it has multiple actions. From "Unlearned" status page I clicked the star icon button, it changed to the custom deck, but the custom status count was "Unlearned: 30 | Unsure: 10 | Learned: 49" when deck count is "Card 1 / 1", I clicked the same Star icon button again and it says :Unlearned: 1 | Unsure: 0 | Learned: 0 as intended. When I click it, i do see a flash of the other "Unlearned: 30 | Unsure: 10 | Learned: 49" and after a few clicks it even stays "Unlearned: 30 | Unsure: 10 | Learned: 49" on the screen, meanwhile the custom card mark star icon turns off then on while the status does its glitch 

I want "health check" in "quick actions" to be the default information shown. Version isnt showing in "User:****" drop down. 

Once again on 2nd attempt no edit cards option was added to edit deck at all. I want in an expanded upon request: In "Edit Decks">decks edit button>add current page to "Edit Deck" tab (default) and add a tab "Edit Cards", inside "Edit Cards" tabs page Search/filter/select from list Per-card actions: Edit (term/definition/pron/group) + Remove(sets status to deleted and add deleted card to deleted page on "Edit Decks" main page with restore,...All Edited cards will also go to Deleted page, but under cards edited, with a "clear" option just for edited cards history. Same goes for bulk edited terms. preview list before applying....*Parts not verified to work* 

Remove dot on deck list, since the dot has no action and we have a switch button and there is an "active" text on the active deck, keep the active deck highlighted as it is.

Edit Deck>Edit Cards>Card "Edit">form. Needs to be fixed to be formatted similar as the dropdown list looks in "In Custom Set" 2nd picture, without the accordion, while preserving the "edit Cards" list functionality, buttons, fields, etc. Check marks to top left corner, "term"/"definition"/"pron..."/ "groups" fields, "Save" and "Cancel" buttons fitting properly in box of activate edit page. This needs to also follow the orientation and device format already established. Keeping "Select all...", "Clear selection", "State-Only", and "Delete selected"(on confirmation) buttons and "# Selected" where they are now. No version bump, just zip name to add 8.6.2.1

add "restore" button to ony "Shown deleted" cards in "Edit cards" list.State-only definition templete dosent even work, so I want to fix it by renaming button to a. Ai template. When clicked, it opens a small form page field that you type into for ai to change selected cards according to what you tell it. Example: I want the definitions to only have the state (i.e "Capital of Texas" as edited to "Texas") Showing in that form page an example output of edited "Term", "definition", etc, only changing the field selected in the form. This over rides any active setting "Short terms only", etc.

Clear search results with an x and cards checked when edit cards windows is closed/exited.  Edit cards search and checks persists.

Clear text inputs once "x" is selected or when window is exited view I want this to be a universal request for all fields in the project. Make sure all alerts show on the correct active page, and to stop persisting I want this to be a universal request for all fields in the project.

When clicking AI Template without selecting a card the alert notification text is on the main edit decks page, which is under the edit card page activily open. Move that alert to the proper page within edit cards page. The text needs to eventually disapear instead of persisting.

I wanted the added close button on same line of Edit deck and Edit Cards button, but on right side of only showing on edit card page, keeping its current smaller size. 

This admin system app information section works on webserver project version. but does not here. On an exe installed version built from packaged I want in also want in the "version" field its Web Server Version 8.5.1 (build 54) its code coming from webserver project and the local EXE version from webserver packaged that is currently showing 6.0.0 (build 18). need to see how we can add Web Server Version in the build to be shown here and to fix the Released not showing up on this exe local version either.  Same needs to be applied to the version area of User:**** drop down.
App Version: 6.0.0 (build 18)
WebAppServer: 8.5.1 (build 54)

1.) I want when users types specific instructions on how he want their terms/definitions to specifically be resulted, it will follow those instruction, this over rides any "short definition only" setting if on. 
2.) I want a setting in ai page, when on, it will show small button(s) with a drop down that has term and or Definition field so you have the option to show ai specifically the way you want the cards to look on that specific study deck, or card when editing cards. off as default
3.) Another setting when on; When using ai and keywords and or name/description are/is filled I want there to be an example showing before performing search. On as default


2. Any status changed to Custom deck while studying does not immediatly show in Unlearned: 11 | Unsure: 1 | Learned: 1 unless I refresh page.  
3. Custom Deck doesnt load after more than available random cards were selected to be added, not even the minimum available was added at all.png

Edit Deck>AI Generator>AI Deck Generator>Keywords> example preview does not show any example output. Use Edit Cards>AI Template> as an example of how I want it to work, does a really good job at it. Fix the AI generator.

Edit Cards>AI Template>Fields to change: Drop down needs to be formatted more like Edit Decks>Add Cards>Target Deck: But with These options Definition(default), Terms, Definition + Term, Pronunciation, Group.  I want the width of the dropdown to be reduced by half its size. 





